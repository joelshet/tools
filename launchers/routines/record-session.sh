#!/bin/sh
# record-session — pre-flight the recording rig, open the checklist and OBS.
# The real script lives in ~/repos/recording-rig/scripts/record-session.sh.
exec "$HOME/repos/recording-rig/scripts/record-session.sh"
